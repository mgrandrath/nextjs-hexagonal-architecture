# `src/pages/` directory

Next.js assumes that a directory called `pages/` or `src/pages/` contains routes
with the old Pages Router syntax. Because this behavior cannot be disabled we
simply use the name `src/screens/` instead.
